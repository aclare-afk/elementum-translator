import type { NextConfig } from "next";

const config: NextConfig = {
  reactStrictMode: true,
  // Each mock lives at /demos/<slug>. The root page lists all demos.
  // No custom rewrites — App Router handles everything.
};

export default config;
